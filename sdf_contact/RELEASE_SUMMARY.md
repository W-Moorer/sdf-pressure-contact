# Final Best Release Summary

Current recommended default stack:

- formal endpoint contact evaluator
- flat support area prior for large flat contacts
- tuned default solver with midpoint + central FD + adaptive substepping
- `step_end` as the best-supported first-contact reporting quantity on current benchmarks

Archived experimental branches are preserved under `experiments/`.
