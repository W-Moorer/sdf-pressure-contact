# Experiments Archive

This directory keeps branches that were implemented and benchmarked during development, but which are **not** the current recommended default.

Typical reasons a branch was archived here:

- it improved diagnosis but not final accuracy
- it was useful for understanding first-contact behavior
- it did not consistently outperform the simpler `step_end` baseline on the current benchmark definition

These experiments are intentionally preserved for reproducibility.
